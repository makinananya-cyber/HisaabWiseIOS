# CLAUDE.md — HisaabWiseIOS

The workspace-level `../CLAUDE.md` holds the project rules, invariants, and iOS
conventions for this repo. Read it first; this file only records per-repo
agent-skill configuration.

## Agent skills

### Issue tracker

GitHub Issues in `makinananya-cyber/HisaabWiseIOS`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
